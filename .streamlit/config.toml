[theme]
base = "light"
primaryColor = "#0a6e5c"
backgroundColor = "#e8f0ec"
secondaryBackgroundColor = "#ffffff"
textColor = "#132421"
font = "sans serif"

[server]
headless = true
# Cursor/沙箱常占满 inotify；禁用事件监听避免 EMFILE，改代码后手动刷新或重启
fileWatcherType = "none"

[browser]
gatherUsageStats = false
